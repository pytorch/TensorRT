[build-system]
requires = [
    "setuptools>=68",
    "wheel>=0.40",
    "pyyaml",
    # Native ABI dependencies supplied by the active Torch-TensorRT build
    # environment.
    # Builds must use --no-build-isolation; see README.md.
    "torch",
    "executorch==1.6.0.dev20260914",
]
build-backend = "setuptools.build_meta"
